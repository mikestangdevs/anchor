import type { EmbeddingProvider } from '@acr/types';

/**
 * Re-export of the EmbeddingProvider interface.
 * This file exists as documentation — the canonical interface
 * lives in @acr/types. Provider adapters implement it.
 */
export type { EmbeddingProvider, RerankerProvider, ExtractionProvider } from '@acr/types';
